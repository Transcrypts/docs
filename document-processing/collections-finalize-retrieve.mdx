---
title: "Get Collection Finalization Results"
description: "Retrieve the finalization results for a document collection"
api: "GET /v1/documents/collections/{collection_id}/finalize/"
---

# Get Collection Finalization Results

This endpoint retrieves the finalization results for a document collection. It provides information about users involved, their associated links, and connected documents.

<Note>
The response does not include a top-level status field. Instead, examine the presence and structure of users, links, and documents to determine the result state.
</Note>

## Authentication

This endpoint requires API credentials:
- **ClientID** (header)
- **AccessKey** (header)

## Path Parameters

| Parameter | Type | Description |
|-----------|------|-------------|
| `collection_id` | string | The unique identifier for the collection (required) |

## Response (200 OK)

The endpoint returns finalization results organized by:

- **Users**: Individuals involved in the collection
- **Links**: Account connections associated with each user
- **Documents**: Files linked to specific accounts

```json
{
  "users": [
    {
      "id": "a1b2c3d4e5f6478899aabbccddeeff00",
      "external_user_id": "ext_user_789",
      "links": [
        {
          "link_id": "c4d5e6f7a8b9c0d1e2f3a4b5c6d7e8f9",
          "status": "done",
          "documents": [
            {
              "id": "d1e2f3a4b5c6d7e8f9a0b1c2d3e4f5a6",
              "document_type": "PAYSTUB",
              "document_subtype": null
            }
          ]
        }
      ]
    }
  ]
}
```

## Error Responses

### 401 Unauthorized

```json
{
  "error": {
    "code": "authentication_failed",
    "message": "No such token"
  }
}
```

### 403 Forbidden

```json
{
  "error": {
    "code": "not_authenticated",
    "message": "Authentication credentials were not provided."
  }
}
```

### 404 Not Found

```json
{
  "detail": "Not Found."
}
```
